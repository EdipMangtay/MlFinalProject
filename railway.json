{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd email_spam_detector && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd email_spam_detector && gunicorn --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120 app:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

